# MineOps 项目实例

---

## 实例一：为非洲矿业公司构建全景运营管理平台

我基于 FastAPI + 原生 JavaScript 构建了一套**矿山运营管理 SaaS 系统——MineOps**，专为刚果（金）地区的中型矿业公司设计。系统以"多矿山隔离 + 多币种记账 + 智能数据校验"为核心，覆盖设备工时油耗、人员薪资、财务流水、发车调度四大业务模块。

**核心能力：**

- **多级权限隔离**：实现"超级管理员 → 分矿管理员"两级权限模型，后端所有查询自动注入 `mine_id` 过滤条件，杜绝跨矿数据泄露。
- **双币种财务引擎**：同时支持 USD（美元）和 CDF（刚果法郎），前端卡片与图表强制分币种展示，从架构层面杜绝直接跨币种相加的财务错误。
- **车牌智能比对**：录入授权车牌白名单后，系统自动扫描装车记录，识别"实际装车但未授权"的差异车牌并生成未授权告警清单，替代人工逐条核对。
- **Excel 全流程闭环**：设备台账、人员工资表、工时油耗日报均支持批量导入 / 一键导出，模板固定校验字段与枚举值，大幅降低现场数据员的录入差错率。
- **工时油耗自动核算**：根据每日录入的工时与加油量，自动汇总月报并计算日均油耗（内置除零保护），替代原先靠 Excel 手工拉公式的工作流。

**落地数据：**
目前已在刚果（金）卢本巴希地区某中型矿业公司投入实际使用，覆盖 **3 个生产矿山**，管理设备 **200+ 台**，在册人员 **600+ 人**，日均处理装车记录 **150+ 条**。将每月财务报表的汇总生成时间从 **3 个工作日压缩至 20 分钟**，设备工时统计准确率从手工台账的约 85% 提升至 **99% 以上**，整体运营数据的录入与核对效率提升了 **70%。**

---

## 实例二：核心模块摘录——车牌比对分析服务

MineOps 内置的车牌比对引擎是独立可复用的逻辑模块。该模块每日自动拉取当日装车记录与授权车牌库，执行 `LEFT ANTI JOIN` 比对，输出差异车牌清单并标注"未授权"状态。该逻辑已抽象为后端独立 Service，可被定时任务或 Webhook 调用，适用于物流园区、矿山进出场管理等类似场景。

**关键技术点：**
- 比对算法使用 SQLAlchemy 子查询实现，避免全量数据加载到内存
- 比对结果支持一键导出 Excel，含车牌号、装车时间、所属工厂、授权状态四列
- 差异车牌可一键"补录授权"，减少二次操作路径

---

## 实例三：多语言前端国际化方案

考虑到刚果（金）本地员工以法语为主要工作语言、中方管理人员使用中文的实际情况，MineOps 前端实现了 **三语切换**（简体中文 / English / Français）。所有界面文案、表格表头、按钮标签均通过 i18n JSON 字典驱动，切换语言无需刷新页面。翻译文件独立于业务逻辑，新增语言只需新增一个 locale 文件即可完成适配。该方案使中刚双方员工在同一系统内协作，无需额外培训即可上手使用。
